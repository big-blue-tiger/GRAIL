#### 临时提示词
data/hf_dataset/data_update/data/pickup_table_cleaned_succeeded/vis/pickup_table__alcohol_12__001.mp4
此数据为较为合适的拼接示例

### output转为qpos
```bash
cd /home/tide/robot/kimodo
python -m kimodo.scripts.motion_convert \
  data/kimodo/demo/output.npz \
  data/kimodo/demo/qpos.csv \
  --from kimodo --to g1-csv
```
### 导出kimodo约束
```bash
conda activate grail

python grail/walk_data_tool/export_kimodo_end_frame.py \
  --speed 0.5 \
  --seed 42 \
  --overwrite
```